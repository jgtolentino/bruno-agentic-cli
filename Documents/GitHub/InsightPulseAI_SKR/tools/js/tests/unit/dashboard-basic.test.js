// Basic test to demonstrate framework is working
describe('Basic dashboard tests', () => {
  test('Truth is truth', () => {
    expect(true).toBe(true);
  });
  
  test('Math works correctly', () => {
    expect(1 + 1).toBe(2);
  });
});