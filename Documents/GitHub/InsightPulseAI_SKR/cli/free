#!/bin/bash
# Launch Pulser interactive shell

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(dirname "$SCRIPT_DIR")"
SHELL_SCRIPT="${REPO_ROOT}/scripts/pulser_shell_enhanced.sh"

# Run the enhanced pulser shell
"$SHELL_SCRIPT"